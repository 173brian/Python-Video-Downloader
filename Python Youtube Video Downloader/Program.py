from GUI import*

instance = GUI(500, 500) # creates instance of window
instance.createWidgets() # fills window with relevant functions
instance.loadWindow() # the loop to provide window with functionality